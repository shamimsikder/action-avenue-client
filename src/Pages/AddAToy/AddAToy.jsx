import React from 'react';
import useTitle from '../../Hooks/useTitle';

const AddAToy = () => {

    useTitle('Add A Toy')

    return (
        <div>
            <h1>Add A Toy</h1>
        </div>
    );
};

export default AddAToy;