import React from 'react';
import useTitle from '../../Hooks/useTitle';

const SignUp = () => {

    useTitle("Sign Up")

    return (
        <div>
            <h1>Sign Up</h1>
        </div>
    );
};

export default SignUp;