import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Blogs = () => {

    useTitle("Blogs")

    return (
        <div>
            <h1>Blogs</h1>
        </div>
    );
};

export default Blogs;